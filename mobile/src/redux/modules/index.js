import { combineReducers } from 'redux'

import example from './example'
import navigation from './navigation'
import review from './review'

export default combineReducers({ example, navigation, review })
